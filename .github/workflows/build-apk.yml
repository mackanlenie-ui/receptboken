name: Bygg Min Dagbok
on: [workflow_dispatch, push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Rätta Android framework-import
        run: sed -i 's/import android\.app\.BiometricPrompt;/import android.hardware.biometrics.BiometricPrompt;/' app/src/main/java/se/steffy/receptboken/DiaryMainActivity.java
      - name: Bygg Min Dagbok
        run: gradle :app:assembleDebug
      - name: Döp APK
        run: cp app/build/outputs/apk/debug/app-debug.apk Min-Dagbok-v1.0.apk
      - uses: actions/upload-artifact@v4
        with:
          name: Min-Dagbok-APK
          path: Min-Dagbok-v1.0.apk
