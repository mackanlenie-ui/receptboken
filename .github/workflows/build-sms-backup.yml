name: Bygg SMS Backup APK
on:
  push:
    branches: [sms-backup-usb]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - run: gradle :smsbackup:assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: SMS-Backup-USB-APK
          path: smsbackup/build/outputs/apk/debug/smsbackup-debug.apk
